<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./assets/css/style.css">
    <title>Farm Budget Planner</title>
</head>
<body>

  <header>
    <h1>Farm Budget Planner!</h1>
  </header>

  <main>
    <div>
        {{{body}}}
    </div>
  </main>

  <nav>
    <a href="/">Home</a>
    {{#if loggedIn}}
    <button id="logout">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
  </nav>


{{#if loggedIn}}
  <main>
    <aside>
        <button id="addFarmBtn">Add New Farm</button>
    </aside>
{{/if}}

    

  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>